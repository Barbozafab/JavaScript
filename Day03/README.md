# Day03 - Booleans, Operators, Dates

* Booleanos
    * `"Verdadeiros"`
    * `"Falsos"`

* Operadores

* Métodos da janela
    * `window.alert()`
    * `window.prompt()`
    * `window.confirm()`

* Objeto Date
